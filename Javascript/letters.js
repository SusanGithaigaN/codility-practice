function longest(s1, s2) {
    //   assign a to z to a variable
    const letters = (/^[a-z\s]+$/);
    //   return new string with distinct letters?
    newStr= letters.map(s =>s);
    return newStr;
  }